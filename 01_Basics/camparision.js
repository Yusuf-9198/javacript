// console.log(2>=1) // true

/*  NOT GIVE PRIDICTATBLE OUTPUT
console.log("2" > 1) // true[because js convert "2"=> 2(number)]
console.log("1"==1)  // true [ "== " -> kah skte ho ki only check value]
console.log("1"===1)  // false [ "===" -> kah skte ho ki  check valuse and datatype]

// Null Trap:-
// Comparisons convert null to the number 0. That is why null >= 0 is true (0>=0).
console.log(null > 0) ; // false
console.log(null == 0); // false
//Equality does not convert null to a number. It only treats null as equal to undefined. That is why null == 0 is false.
console.log(null >= 0);  // true[null => 0(number)]
console.log(undefined >= 0);  // alway false
// undefined is converted to NaN (Not a Number) during comparison. Any comparison with NaN (greater, less, or equal) will always result in false. 
console.log(null == undefined); // true
console.log(null === undefined); // false

*/


